# Normalise line endings so the repo stays LF regardless of contributor platform.
* text=auto eol=lf

# Binary-ish assets should never be normalised.
*.png binary
*.jpg binary
*.webp binary
*.wasm binary
